import React, { FC } from "react";

import styles from "./Select.module.scss";
import { NavMenuProps } from "./NavMenu.props";

const NavMenu: FC<NavMenuProps> = () => {
  return <div className="navmenu">Select</div>;
};

export default NavMenu;
